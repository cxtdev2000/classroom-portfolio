import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Bottom-left is taken by the background-music controls.
  devIndicators: { position: "bottom-right" },
};

export default nextConfig;
